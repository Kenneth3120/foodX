{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",       // Ensures the app starts normally
    "build": "rm -rf node_modules && npm install", // Rebuilds bcrypt and all dependencies
    "server": "nodemon server.js"    // Optional for development use only
  },
  "dependencies": {
    "bcrypt": "^5.1.1",              // Keep bcrypt if you want to rebuild it
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.6.3",
    "multer": "^1.4.5-lts.1",
    "paytmchecksum": "^1.5.1",
    "stripe": "^16.12.0",
    "validator": "^13.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"              // Optional, for local development
  }
}
